{
  "name": "fin-twin-monorepo",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "db:generate": "pnpm --filter @fin-twin/db db:generate",
    "db:push": "pnpm --filter @fin-twin/db db:push",
    "db:seed": "pnpm --filter @fin-twin/db db:seed"
  },
  "devDependencies": {
    "turbo": "^2.4.4",
    "typescript": "^5.8.2"
  },
  "packageManager": "pnpm@11.5.2"
}
